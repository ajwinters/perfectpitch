---
title: "R Notebook"
output: html_notebook
---


1. likilihood of getting a note correct based off distance from last note
2. liklihood of getting a note correct based on how long ago the same note of any octave was played
3. average correct on first guess 
4. average number of guesses per attempt over time
5. more correct guesses as the session goes on
6. notes which are most likely to get correct

```{r}
library(lubridate)
```



```{r}
df <- read.csv("C:/Users/Alex/Projects/perfectpitch/data_v2.csv")

```


```{r}
df$Note <- df$CorrectNote %% 12
df$timestamp <- as.POSIXct(df$time,format =  "%m/%d/%Y %H:%M")
df$date <- as.Date(df$time,format =  "%m/%d/%Y %H:%M")

```

```{r}
df
```



```{r}
df
```






